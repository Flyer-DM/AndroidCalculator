<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Calculator" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.Calculator" parent="Base.Theme.Calculator" />

    <style name="BaseButtonStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:padding">16dp</item>
        <item name="android:textSize">18sp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/rounded_button_selector</item>
    </style>

    <!-- Стиль для кнопок с цифрами и точкой -->
    <style name="NumberButtonStyle" parent="BaseButtonStyle">
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Стиль для кнопок операций -->
    <style name="OperationButtonStyle" parent="BaseButtonStyle">
        <item name="android:textColor">@color/orange</item>
    </style>

    <!-- Стиль для кнопок памяти (M+, MR, MC) -->
    <style name="MemoryButtonStyle" parent="BaseButtonStyle">
        <item name="android:textColor">@color/blue</item>
    </style>

    <!-- Стиль для кнопки равно -->
    <style name="EqualButtonStyle" parent="BaseButtonStyle">
        <item name="android:textColor">@color/orange</item>
    </style>

</resources>